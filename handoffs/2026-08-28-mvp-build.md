# Session handoff — MVP build, 2026-08-28

Workstream closed at a clean boundary: MVP sealed. Full machine facts in
[ENGINEER-HANDOFF.md](../ENGINEER-HANDOFF.md) (receipt); this file adds
only decisions, exceptions, blockers, and next actions.

## State

HEAD = final docs commit on main (local repo /Users/galangster/Projects/
wren, no remote). All wayfinder tickets closed (T1–T7). All gates green at
seal. Captures current in docs/captures/, sent to Nick in-session.

## Open owner gates (Nick)

1. **Visual approval** — captures sent; UI is not "done" until approved
   (standing rule). Feedback → a fresh session runs a delta polish lane.
2. **Google OAuth client** — create per docs/SETUP-GOOGLE-OAUTH.md, paste
   into Settings → Google API, Add account. First live sync is the next
   real verification gate; watch for quota backoff logs and the 7-day
   Testing-status re-auth.
3. **GitHub remote** — push, then run the Windows workflow
   (workflow_dispatch) and smoke the NSIS installer on a Windows machine.
4. **Anron icons** — export SVGs from Figma (or connect a Figma MCP);
   swap inside src/components/ui/icon.tsx only.

## Exceptions / deviations logged

- React over the §10 Svelte doctrine — Nick delegated the call
  (docs/DECISIONS.md Round 4); flagged, reversible at the MailService seam.
- Ultracode was on; per Nick's token-efficiency contract the build ran
  bounded sequential lanes (7 implementation/review agents + 2 simplify
  reviewers + 1 research), not workflow fan-outs.
- wayfinder/grill-me ran with the skill's execution override + one live
  HITL round (Round 4); later decisions self-resolved per Nick's
  "decide autonomously" ruling.

## Resume prompt (fresh session)

```
Open /Users/galangster/Projects/wren. Read handoffs/2026-08-28-mvp-build.md
and ENGINEER-HANDOFF.md, then take the next open owner-gate item Nick names
(visual feedback, live-Gmail verification, Windows CI run, or Anron swap).
Contracts: src/core/types.ts is the UI seam; docs/design/DIRECTION.md is
the visual law; docs/SOP.md governs delegation.
```
